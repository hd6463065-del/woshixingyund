明白，你想把这句话精简一下，直接表达“更新件数为0时，事务回滚”。

用日语可以这样写：

「更新件数が0件の場合、トランザクションをロールバックします。」
(当更新件数为0时，回滚事务。)

💡 小提示：
你打字的时候可能手滑打成了“零件”，我猜你想说的是“0件”对吧？（在日文IT语境里，通常用「0件（ゼロけん）」来表示记录数为0）。

另外，日文里“事务”对应的汉字是「トランザクション」（外来语），而不是“事物”哦。

如果你希望日志更简短一点，也可以直接用这句：
「更新件数0件のため、ロールバックします。」
(因更新件数为0，执行回滚。)

这两句都非常标准